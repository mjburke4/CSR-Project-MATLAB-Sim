# T19 implementation contract

Root working tree: `csr19`, copied from repaired accepted `csr18`. Do not modify csr18. Current native main freshly checked remains 486d9e01f010fdfd4c6aebb87c6d7e51fc674a5b; engine6b5cd24ea80713ce16d88575869aedd6f432bdae.

Objective: DATA queued-retry expiration, capacity release and per-flow admission/delivery effects under complete original seven-node campus traffic. User wants practical agreement within a few percent, not exact stochastic traces. Descriptive ±5% native total and per-flow delivery/admission bands, no numerical failure gate.

Two required fresh cases: a128 = actual-tx default, p128 = native-provisional experimental. Both use original campus_multihop_6000 input file/hash, seed128, horizon6000, full seven nodes and six flows, real PHY/autonomous routing/default continuous timing, zero poststop drain. Same source runtime and seed within MATLAB; changed policy can change subsequent RNG consumption, not strict common-random-number coupling. Single-seed effect only; full-horizon extra seed deferred until a useful effect exists.

Core interface: Config.Hop.DataQueuedRetryPolicy one of actual-tx(default), native-provisional. DATA only; CONTROL unchanged. Modify only +csr/+hop/Layer.m and +csr/+hop/validateConfig.m, plus new test. Allowed prior-source change bindings final root candidate hash exact.

Observations: no additional observer or scheduler callbacks. Original protocol+PHY traces complete with each limit1500000. Original application admission prefix limit100000 retained; exact total/per-flow attempt counters and all admitted application identities and events remain complete. Existing protocol events support postcallback HOP DATA capacity ledger (hop_admit acquires, hop_ack/hop_failed/hop_dack_expired release; hop_dack holds); NWK enqueue/custody_release tracks per-origin NSDP. Explicitly label reconstructed state, not hidden queue polling. DATA-only events filter FrameKind; service ledger joins PacketId plus node,peer,hopsequence; repeated queued retry requests must remain separate and cannot assume every retry has a TX. Initial/actual TX via hop_sent. T19 experimental unmatched-TX fallback may need callback detail; coordinate core agent.

Plan file scenarios/t19/plan.json: root produces. Fields schema='csr-tranche19-queued-retry-plan-v1', tranche19, ns3_source_commit, engine_commit, scenario_file+scenario_sha256 original, baseline_owner_evidence='evidence/t17/owner.zip' (SHA bound), native_reference_directory='evidence/tranche-7-ns3-reference/campus_multihop_6000', cases[{case_id,policy,seed,duration_s,scenario,scenario_file,scenario_sha256,reference_directory,bucket_width_s=60}], execution_order=['a128','p128'], planned_simulated_seconds12000, comparison_band_percent5.0, trace_limits protocol1500000/phy1500000/admission100000, max_events12000000, expected_admission_attempts1710000 percase. Scope flags default_policy_changed false, phy_ecc_changed false, observer_enabled false, post_horizon_drain false, full_portable_regression true, numerical_parity_required false, common_random_numbers_claimed false, single_seed_scope true. More fields allowed only explicit coordination.

Runner: run_tranche19_validation(outputRoot,options), default fixed results/t19, options.Phase all|tests|a128|p128|finalize. Stages tests (full top-level portable suite; native subfolder excluded), a128, p128; new stage receipt helper `csr.validation.RetryPolicyCheckpoint` (T17-style immutable completed receipts; no changes to old ReleaseCheckpoint). New `csr.scenario.tranche19Suite`; `csr.validation.retryPolicyContract` runs/exports one case. Stage manifest path a128/case.json or p128/case.json; raw/analysis siblings. All cases use exact original CSV and config inherited with ONLY DataQueuedRetryPolicy addition/change. No new Benchmark case ID necessary: keep original config Benchmark metadata same as T17 to allow default regression comparison.

Metadata Schema csr-matlab-tranche-19-validation-v1; Status completed-review-required when tests+both cases complete, otherwise phase-completed-review-pending or failed. CandidateFile evidence/tranche-19-candidate.json; Case manifests schema csr-tranche19-queued-retry-case-v1. Root-level source.json,references.json,candidate.json,plan.json; tests/results.csv; original-style raw case_manifest.json from exportResearchCase; t19.zip return excludes .mat and nested archive files. Preserve failures and isolate partial stage; never silently resume partial simulation. Completed-case reuse requires exact candidate/source/references/runtime and artifact bytes. Unrelated output directories rejected before writes.

Candidate root creates once all authors finish: Schema csr-tranche-19-candidate-v1, Tranche19, SourceCommit, BaselineSourceSnapshot evidence/tranche-19-baseline.json (346 T18 source bindings), BaseSourceSnapshotSHA256, BaselineSourceFiles/MatlabFiles counts, AllowedModifiedSourceFiles as records {path,baseline_sha256,candidate_sha256}, SourceFiles (sourceSnapshot excluding self), SourceFilesExcludedPaths=[self], ReferenceFiles list, ReferenceFileInventory records {path,sha256,bytes}, ReferenceRoots=[], Plan+PlanSHA256, TestFiles full top-level list, ExpectedTestNames. Baseline T18 source modifications limited to two approved HOP files. Owner evidence/acceptance source identifies accepted parent in evidence/t18.

Checker: new scripts/analyze_tranche19_return.py and tranche19_metrics.py; immutable original owner/candidate bytes; copied T18 parsing repair is evidence only, no additional old sources modified. Checker binds all artifacts/sources/references/tests/stages, validates exact configuration deltas, complete raw accounting and traces, full default-vs-T17 raw/statistics regression (exclude timing-only runtime metadata/config policy field). Analyze 300s bins and whole-run totals/per-flow counts; baseline vs experimental within MATLAB, both vs pinned native. Failure to be within5% descriptive not acceptance failure. Do not use old MATLAB T18 cohort analyzer that rejects overlapping queued retries for experimental policy.

No MATLAB runtime here. Root packages overlay onto copied accepted T18 install and saves t19up.zip. Agent ownership recorded separately; coordinate schema changes directly and notify root.
