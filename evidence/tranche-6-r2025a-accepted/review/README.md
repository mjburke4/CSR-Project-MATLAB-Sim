# Review execution snapshot

`integrity-audit-snapshot.py` preserves the exact independent audit procedure.
Its absolute paths and HEAD check describe the review workspace before the
evidence commit; it is a historical execution record, not a portable CLI.
Use the retained ZIP, validated commit `21c0a3f`, and source snapshot identities
when reproducing the checks in a different checkout.
