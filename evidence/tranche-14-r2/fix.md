T14 r2 repairs table shape in the new diagnostic. MATLAB struct2table had inferred N-by-width character matrices for same-width strings, making the different-width case labels fail at cross-case concatenation and exposing single characters during fixed-width hexadecimal indexing. All six output tables now use explicit N-by-1 string fields before any checks. uint64 IDs and bitmaps, doubles and logical fields keep their declared scalar types, including zero-row outputs.

Three independent MATLAB regressions exercise heterogeneous case names, equal-width hexadecimal values, exact uint64 CSV round trips, real TraceScheduler zero/one/multiple records and ReplayStreams table normalization at the T14 boundary. The focused suite now contains 109 tests in 10 classes; 222 existing structural checks are retained. Existing six-case checks additionally assert all text column shapes and per-case evidence.

Each case saves six CSV files and result.json to short c1–c6 subfolders before aggregation. Caught case errors print immediately. The validated production baseline, ReplayStreams and EventScheduler are unchanged. All five changed/new MATLAB files pass MISS_HIT lint; MATLAB execution remains pending.
